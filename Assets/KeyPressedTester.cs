using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class KeyPressedTester : MonoBehaviour
{
    // Update is called once per frame
    void Update()
    {
        // if(Input.anyKey){
        //     print(Input.inputString); // prints the key pressed
        // }

        // print("Horiz:" + Input.GetAxisRaw("Horizontal")); // prints the value of the horizontal axis (left/right arrow keys)
        // print("Vert:" + Input.GetAxisRaw("Vertical")); // prints the value of the vertical axis (up/down arrow keys)
    }
}
